apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx         # Matches BOTH stable and canary
    # No track label here - routes to both!
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort
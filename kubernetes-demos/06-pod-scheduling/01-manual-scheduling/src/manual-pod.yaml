apiVersion: v1
kind: Pod
metadata:
  name: pod-manual
spec:
  nodeName: 3node-m03    # ← you set this, not the scheduler
  containers:
    - name: busybox
      image: busybox
      command: ["sh", "-c", "sleep 3600"]

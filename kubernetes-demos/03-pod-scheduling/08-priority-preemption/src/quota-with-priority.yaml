apiVersion: v1
kind: ResourceQuota
metadata:
  name: critical-quota
  namespace: default
spec:
  hard:
    pods: "2"
    requests.cpu: "4"
    requests.memory: "4Gi"
  scopeSelector:
    matchExpressions:
      - operator: In
        scopeName: PriorityClass
        values:
          - critical

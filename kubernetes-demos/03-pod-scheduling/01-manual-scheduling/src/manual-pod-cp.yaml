apiVersion: v1
kind: Pod
metadata:
  name: pod-manual-cp
spec:
  nodeName: 3node    # ← control plane node
  containers:
    - name: busybox
      image: busybox
      command: ["sh", "-c", "sleep 3600"]

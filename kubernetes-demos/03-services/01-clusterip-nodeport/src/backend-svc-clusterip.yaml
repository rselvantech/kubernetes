apiVersion: v1
kind: Service
metadata:
  name: backend-svc
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 9090        # service listens on 9090
      targetPort: 5678  # container listens on 5678
      protocol: TCP

apiVersion: v1
kind: Pod
metadata:
  name: restart-always-demo
spec:
  restartPolicy: Always       # Default — can be omitted, shown explicitly for clarity
  containers:
    - name: nginx
      image: nginx:1.27
      ports:
        - containerPort: 80
apiVersion: v1
kind: Service
metadata:
  labels:
    app: prod-wordpress
  name: prod-wordpress
spec:
  ports:
  - nodePort: 30001
    port: 80
  selector:
    app: wordpress
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: prod-wordpress
  name: prod-wordpress
spec:
  selector:
    matchLabels:
      app: prod-wordpress
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: prod-wordpress
    spec:
      containers:
      - image: wordpress:4.8-apache
        name: wordpress
        ports:
        - containerPort: 80
          name: wordpress
        volumeMounts:
        - mountPath: /var/www/html
          name: wordpress-persistent-storage
      volumes:
      - emptyDir: {}
        name: wordpress-persistent-storage
